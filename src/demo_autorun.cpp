// Shared one-shot autonomous demo entry point. PlatformIO selects G1 or the
// standard square G2 with AUTORUN_PRESET_PATH; vehicle_profile.h supplies only
// the selected vehicle's calibrated parameters and path approval.
#include "main.cpp"
